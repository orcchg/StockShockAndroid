plugins {
    id("convention.kotlin-android-library")
    id("kotlin-kapt")
}

dependencies {
    api(
        project(':core:context:api')
    )
    implementation(
        libs.dagger
    )
    kapt(
        libs.daggerCompiler
    )
}
