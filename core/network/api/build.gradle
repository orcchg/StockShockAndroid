plugins {
    id("convention.kotlin-jvm")
}

dependencies {
    api(
        project(':core:di')
    )
    implementation(
        libs.moshi,
        libs.moshiKotlin,
        libs.retrofit,
        libs.rx,
        libs.scarlet
    )
}
