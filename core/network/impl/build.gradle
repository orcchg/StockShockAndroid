import com.orcchg.yandexcontest.build.Dependencies

apply plugin: Dependencies.Plugin.androidLib
apply plugin: Dependencies.Plugin.kotlinAndroid
apply plugin: Dependencies.Plugin.kotlinAnnotation

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    api(
            project(':core:network:api')
    )
    implementation(
            project(':core:context:api')
    )
    implementation(
            Dependencies.Di.dagger,
            Dependencies.Lib.ktStdLib,
            Dependencies.Network.okHttpLog,
            Dependencies.Network.retrofit,
            Dependencies.Network.retrofitMoshi,
            Dependencies.Network.retrofitRx,
            Dependencies.Network.scarlet,
            Dependencies.Network.scarletAndroid,
            Dependencies.Network.scarletMoshi,
            Dependencies.Network.scarletOkHttp,
            Dependencies.Network.scarletRx,
            Dependencies.Network.stethoOkHttp,
            Dependencies.Parser.moshi,
            Dependencies.Parser.moshiKotlin
    )
    debugImplementation(
            Dependencies.Network.chucker
    )
    releaseImplementation(
            Dependencies.Network.chuckerRelease
    )
    kapt(
            Dependencies.Di.daggerCompiler,
            Dependencies.Parser.moshiKotlinCodegen
    )
}
