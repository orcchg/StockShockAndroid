plugins {
    id("convention.kotlin-android-library")
    id("kotlin-kapt")
}

dependencies {
    api(
        project(':core:schedulers:api')
    )
    implementation(
        libs.dagger,
        libs.rx,
        libs.rxAndroid
    )
    kapt(
        libs.daggerCompiler
    )
}
