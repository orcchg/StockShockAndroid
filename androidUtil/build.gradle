plugins {
    id("convention.kotlin-android-library")
}

dependencies {
    implementation(
        project(':util')
    )
    implementation(
        libs.appCompat,
        libs.fragmentKtx,
        libs.lifecycle,
        libs.liveData,
        libs.navigation,
        libs.recyclerView,
        libs.rx,
        libs.rxAndroid,
        libs.viewPager2
    )
}
