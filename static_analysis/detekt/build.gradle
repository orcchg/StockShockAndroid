import com.orcchg.yandexcontest.build.Dependencies

apply plugin: Dependencies.Plugin.kotlin

dependencies {
    implementation Dependencies.Lint.detektApi

    testImplementation(
            Dependencies.Lint.detektTest,
            Dependencies.Test.junit
    )
}
