plugins {
    id("convention.kotlin-android-library")
}

android {
    namespace("com.orcchg.yandexcontest.search_bar.ui")
    resourcePrefix = "search"

    buildFeatures {
        viewBinding true
    }
}

dependencies {
    api(project(":design"))
    implementation(project(":androidUtil"))
    implementation(
        libs.androidx.constraintlayout,
        libs.androidx.core.ktx,
        libs.androidx.material,
        libs.library.rx.binding
    )
}
