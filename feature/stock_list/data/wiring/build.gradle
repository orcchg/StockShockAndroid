plugins {
    id("convention.kotlin-android-library")
    id("org.jetbrains.kotlin.kapt")
}

dependencies {
    api(project(":feature:stock_list:data:api"))
    implementation(
        project(":core:context:api"),
        project(":core:feature_flags:api"),
        project(":core:network:api"),
        project(":core:schedulers:api"),
        project(":feature:stock_list:data:fake") // switch data layer impl
    )
    implementation(
        libs.androidx.core.ktx,
        libs.androidx.room,
        libs.debugging.timber,
        libs.di.dagger,
        libs.library.retrofit,
        libs.library.rx,
        libs.library.scarlet
    )
    kapt(libs.di.dagger.compiler)
}
