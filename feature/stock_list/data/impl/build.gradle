plugins {
    id("convention.kotlin-android-library-room")
    id("org.jetbrains.kotlin.kapt")
}

android {
    namespace("com.orcchg.yandexcontest.stocklist.data")
}

dependencies {
    api(
        project(":feature:stock_list:data:api"),
        project(":util") // expose :util classes used by :impl for :wiring
    )
    implementation(
        project(":core:feature_flags:api"),
        project(":core:network:api"),
        project(":core:schedulers:api")
    )
    implementation(
        libs.androidx.room.rx,
        libs.debugging.timber,
        libs.di.dagger,
        libs.library.retrofit,
        libs.library.rx
    )
    kapt(libs.di.dagger.compiler)
}
