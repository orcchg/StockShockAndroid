plugins {
    id("convention.kotlin-android-library-room")
    id("org.jetbrains.kotlin.kapt")
}

dependencies {
    api(project(":feature:stock_list:data:impl")) // expose common types of implementation for DI
    implementation(
        project(":core:feature_flags:api"),
        project(":core:network:tiingo"), // configure network for Tiingo
        project(":core:schedulers:api"),
        project(":util")
    )
    implementation(
        libs.di.dagger,
        libs.library.moshi,
        libs.library.retrofit,
        libs.library.rx,
        libs.library.scarlet
    )
    kapt(
        libs.di.dagger.compiler,
        libs.library.moshi.kotlin.codegen
    )
}
