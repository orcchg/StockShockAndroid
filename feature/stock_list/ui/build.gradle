plugins {
    id("convention.kotlin-android-library")
    id("org.jetbrains.kotlin.kapt")
}

android {
    namespace("com.orcchg.yandexcontest.stocklist.ui")
    resourcePrefix = "stock"

    buildFeatures {
        viewBinding true
    }
}

dependencies {
    api(
        project(":design"),
        project(":feature:stock_list:api")
    )
    implementation(
        project(":androidUtil"),
        project(":util")
    )
    implementation(
        libs.androidx.constraintlayout,
        libs.androidx.material,
        libs.androidx.recyclerview,
        libs.di.dagger,
        libs.library.glide,
        libs.library.rx.binding
    )
    kapt(libs.di.dagger.compiler)
}
