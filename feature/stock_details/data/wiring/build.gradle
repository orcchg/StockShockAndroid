plugins {
    id("convention.kotlin-android-library")
    id("kotlin-kapt")
}

dependencies {
    api(
        project(':feature:stock_details:data:api')
    )
    implementation(
        project(':core:context:api'),
        project(':core:feature_flags:api'),
        project(':core:network:api'),
        project(':core:schedulers:api'),
        project(':feature:stock_details:data:finnhub') // switch data layer impl
    )
    implementation(
        libs.coreKtx,
        libs.dagger,
        libs.retrofit,
        libs.room,
        libs.rx,
        libs.scarlet,
        libs.timber
    )
    kapt(
        libs.daggerCompiler
    )
}
