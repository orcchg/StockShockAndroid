plugins {
    id("convention.kotlin-android-library")
}

android {
    resourcePrefix = "stock_details"
}

dependencies {
    api(
        project(':design')
    )
    implementation(
        project(':androidUtil')
    )
    implementation(
        libs.coreKtx,
        libs.material
    )
}
