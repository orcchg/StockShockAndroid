plugins {
    id("convention.kotlin-jvm")
}

dependencies {
    api(
        project(":core:di"),
        project(":core:model")
    )
    implementation(
        libs.di.inject,
        libs.library.rx
    )
}
