plugins {
    id("convention.kotlin-android-library")
    id("androidx.navigation.safeargs.kotlin")
}

dependencies {
    api(
        libs.navigation,
        libs.navigationDynamicFeatures,
        libs.navigationUi
    )
    implementation(
        libs.appCompat
    )
}
