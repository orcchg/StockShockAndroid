plugins {
    id("convention.kotlin-jvm")
}

dependencies {
    api(
        libs.rx
    )
    testImplementation(
        testLibs.hamcrest,
        testLibs.junit
    )
}

compileKotlin {
    kotlinOptions {
        freeCompilerArgs += ["-Xinline-classes"]
    }
}
