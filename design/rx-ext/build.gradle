plugins {
    id("convention.kotlin-android-library")
}

dependencies {
    api(
        project(':design')
    )
    implementation(
        libs.appCompat,
        libs.constraintLayout,
        libs.rxBinding
    )
}
