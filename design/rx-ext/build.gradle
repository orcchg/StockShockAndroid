plugins {
    id("convention.kotlin-android-library")
}

dependencies {
    api(project(":design"))
    implementation(
        libs.androidx.constraintlayout,
        libs.library.rx.binding
    )
}
