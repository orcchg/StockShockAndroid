plugins {
    id("convention.kotlin-android-library")
}

android {
    namespace("com.orcchg.yandexcontest.design.rx_ext")
}

dependencies {
    api(project(":design"))
    implementation(
        libs.androidx.constraintlayout,
        libs.library.rx.binding
    )
}
